# Supabase Vector Database Configuration
# Copy this file to .env and fill in your values

# REQUIRED: Your Supabase project URL
# Find this in: Dashboard → Settings → API → Project URL
SUPABASE_URL=https://your-project-id.supabase.co

# REQUIRED: Your database password (NOT the service key!)
# Find this in: Dashboard → Settings → Database → Database Password
# If you forgot it, you can reset it there
SUPABASE_DB_PASSWORD=your-database-password-here

# REQUIRED FOR API ACCESS: Service key for REST API calls
# Find this in: Dashboard → Settings → API → service_role key
# This is used by the ingestion service and RAG agent
SUPABASE_SERVICE_KEY=your-service-key-here

# Optional: Database configuration
TABLE_NAME=rag_documents
EMBEDDING_DIMENSION=3072

# Optional: PostgreSQL connection settings
PGSSLMODE=require
PGCONNECT_TIMEOUT=10